---
name: doubao2api
description: 豆包多模态 API 技能。当用户要求生成图片、AI 绘图、文生图、图生图、生成视频、生成音乐、AI 识图时使用。触发词包括"生成图片"、"画一张"、"文生图"、"AI绘图"、"豆包生图"、"图生图"、"参考图生图"、"生成视频"、"生成音乐"、"识图"等。
---

# doubao2api - 豆包多模态 API (Minis 技能指南)

逆向豆包（Doubao）内部 API，无需 API Key，利用浏览器 session cookies 免费调用。

## 快速使用

### 图像生成（文生图）
```bash
python3 doubao_api.py image "描述" [--ratio 16:9]
```

### 图像变体（图生图）
```bash
python3 doubao_api.py variation 图片路径 [--prompt "描述"] [--ratio 1:1]
```

### 音乐生成
```bash
python3 doubao_api.py music "描述" [--genre pop] [--mood happy]
```

### 视频生成
```bash
python3 doubao_api.py video "描述" [--ratio 16:9] [--timeout 300]
```

### 聊天
```bash
python3 doubao_api.py chat "你好" [--think]
```

## 功能参数矩阵

| 功能 | content_type | skill_type | 说明 |
|------|-------------|------------|------|
| 聊天 | 2001（返回文本通常为 2018，代码为 2071） | - | 支持深度思考 `--think` |
| 文生图 | 2009 | 3 | 并发下载多张超清候选图 |
| 图生图 | 2009 | 3 | 基于参考图精准锁定人脸换装 |
| 视频生成 | 2020 | 17 | 异步任务流式轮询 |
| 音乐生成 | 2005 | 9 | 支持自定义流派与情绪 |

## 提示词规则与技巧

### 核心原则
- **提示词控制在 10 个字以内**
- **必须包含动作词**，否则姿势保持原图不变
- **不要堆砌描述**，越复杂越不像参考图
- **清晰度**：文生图可加「4K高清」「最高分辨率」「极致清晰度」提升分辨率（864→1152），图生图无效

### 动作词库

| 类别 | 动作词 |
|------|--------|
| **头部** | 正在整理头发、回眸一笑、侧头微笑、歪头杀、轻轻点头 |
| **手部** | 手提裙摆、双手背在身后、托腮、挥手、轻抚脸颊 |
| **腿部** | 坐在台阶上、靠着墙、侧坐在椅子上、盘腿坐、倚靠门框 |
| **身体/镜头** | 正对镜头、背对镜头、侧对镜头 |

### 全量展示原则（铁律）
生图产出的所有候选图（3~4张），必须在聊天框里全部以 Markdown 图片格式排版呈现，方便挑选，禁止只展示单张。
